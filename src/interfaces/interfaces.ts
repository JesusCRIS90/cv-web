export interface IWheelEventData {
  pageX: number;
  pageY: number;
  ClientX: number;
  ClientY: number;
  YScroll: number;
}

export interface ISection {
  id: string;
  sectionName: string;
  icon: string;
  isActive: boolean;
}

